<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Menu</title>
    <%- include('../partials/head') %>

    <link rel="stylesheet" href="/css/base.css">
    <link rel="stylesheet" href="/css/home.css">
    <link rel="stylesheet" href="/css/admin.css">
    
</head>
<body>

    <%- include('../partials/nav') %>

    <main class="page">
        <h1 class="page-title">Edit Menu Item</h1>
    <section class="form-card">
      <form class="form" method="POST" action="/admin/menu/<%= item._id %>?_method=PUT">
        
        <div class="form-group">
          <label for="name">Menu Name</label>
          <input
            id="name"
            name="name"
            type="text"
            value="<%= item.name %>"
            required
          />
        </div>

        <div class="form-group">
          <label for="category">Category</label>
          <input
            id="category"
            name="category"
            type="text"
            value="<%= item.category %>"
            required
          />
        </div>

        <div class="form-row">
          <div class="form-group">
            <label for="price">Price ($)</label>
            <input
              id="price"
              name="price"
              type="number"
              step="0.01"
              value="<%= item.price %>"
              required
            />
          </div>

          <div class="form-group">
            <label for="rating">Rating (1-5)</label>
            <input
              id="rating"
              name="rating"
              type="number"
              min="1"
              max="5"
              step="0.5"
              value="<%= item.rating %>"
              required
            />
          </div>
        </div>

        <div class="form-group">
          <label for="details">Details</label>
          <textarea
            id="details"
            name="details"
            rows="8"
            required
          ><%= item.details %></textarea>
        </div>

        <button class="btn-primary" type="submit">Save</button>

      </form>
    </section>
  </main>

        <a class="back-link" href="/admin/menu">← Back</a>

</body>
</html>
